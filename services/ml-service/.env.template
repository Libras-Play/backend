# ML Service - Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to git!

# =============================================================================
# AWS CONFIGURATION
# =============================================================================
# AWS Region
AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1

# NOTE: In production (ECS), use IAM roles instead of access keys
# Never set AWS_ACCESS_KEY_ID or AWS_SECRET_ACCESS_KEY in .env for production

# =============================================================================
# S3 CONFIGURATION
# =============================================================================
# S3 Bucket for ML models and videos
S3_BUCKET_NAME=libras-play-videos-dev

# Video processing settings
VIDEO_MAX_SIZE_MB=100
ALLOWED_VIDEO_EXTENSIONS=mp4,webm,mov

# =============================================================================
# MODEL CONFIGURATION
# =============================================================================
# ML Model paths (S3 keys)
MODEL_PATH=models/sign_language_model.h5
MODEL_VERSION=v1.0.0

# Model inference settings
CONFIDENCE_THRESHOLD=0.75
MAX_PREDICTIONS=5

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Environment: development, staging, production
ENVIRONMENT=development

# Application name and version
APP_NAME=ml-service
VERSION=1.0.0

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# =============================================================================
# PROCESSING SETTINGS
# =============================================================================
# Video processing timeout (seconds)
PROCESSING_TIMEOUT=300

# Max concurrent requests
MAX_WORKERS=4

# Temporary file storage
TEMP_DIR=/tmp/ml-service

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. NEVER commit this file with real values to git
# 2. Use IAM roles in ECS for S3 access
# 3. Enable S3 bucket encryption
# 4. Use signed URLs for video uploads/downloads
# 5. Implement rate limiting for ML inference
# 6. Monitor model inference costs

# =============================================================================
# PRODUCTION DEPLOYMENT
# =============================================================================
# For ECS deployment, set these as environment variables in task definition:
# - AWS_REGION from task metadata
# - S3_BUCKET_NAME from Terraform outputs
# - ENVIRONMENT=production
# - LOG_LEVEL=WARNING
# - Use IAM task role for S3 access
